fastapi==0.141.1
uvicorn[standard]==0.32.1
nba_api==1.11.4
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2
# Required by app/lineup_model.py — without it the Lineup Optimizer's
# hypothetical predictor fails to load at startup ("Could not load lineup
# snapshot: No module named 'xgboost'") while the UI still advertises it.
xgboost==2.1.3
anthropic>=0.40.0
reportlab>=4.2.0
pydantic-settings==2.6.1
# Per-IP rate limiting for the AI + training endpoints (app/limits.py).
slowapi==0.1.9
httpx==0.28.1
requests==2.33.0
lxml>=5.0.0
