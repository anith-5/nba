# Slim dependency set for cloud deploy (Render free tier).
# Excludes mediapipe + opencv-python-headless — they are large (~hundreds of MB),
# slow to build, and risk exceeding the 512MB free-tier memory limit. The only
# feature that needs them is the Scouting Report video upload, which degrades
# gracefully (shows "video analysis unavailable") when they're absent.
fastapi==0.141.1
uvicorn[standard]==0.32.1
nba_api==1.11.4
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2
# Needed on the deployed site too, not just locally: lineup_model.load_snapshot()
# imports XGBRegressor to read the pre-trained data_cache/lineup_xgb.json. Without
# it the Lineup Optimizer's hypothetical predictor is dead in production while the
# UI still offers it. Heavier than the rest of this list — if the free tier starts
# OOMing, this is the first thing to look at.
xgboost==2.1.3
anthropic>=0.40.0
reportlab>=4.2.0
pydantic-settings==2.6.1
# Per-IP rate limiting for the AI + training endpoints (app/limits.py).
slowapi==0.1.9
httpx==0.28.1
requests==2.33.0
lxml>=5.0.0
