# Slim dependency set for cloud deploy (Render free tier).
# Excludes mediapipe + opencv-python-headless — they are large (~hundreds of MB),
# slow to build, and risk exceeding the 512MB free-tier memory limit. The only
# feature that needs them is the Scouting Report video upload, which degrades
# gracefully (shows "video analysis unavailable") when they're absent.
fastapi==0.115.6
uvicorn[standard]==0.32.1
nba_api==1.11.4
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2
anthropic>=0.40.0
reportlab>=4.2.0
pydantic-settings==2.6.1
python-multipart==0.0.17
httpx==0.28.1
requests==2.32.3
lxml>=5.0.0
