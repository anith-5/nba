# CBA rules package (Phase 2)

Pure Python salary cap and trade validation logic will live here so it can be unit-tested without HTTP.

Planned modules:

- `matching.py` — salary matching rules
- `apron.py` — luxury tax / apron checks
- `picks.py` — draft pick trade restrictions
