{
  "name": "hoopiq-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev:arena": "concurrently -n web,arena -c green,magenta \"npm run dev --prefix apps/web\" \"npm run dev --prefix services/arena-realtime\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
